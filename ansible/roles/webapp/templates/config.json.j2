{
  "server": {
    "host": "127.0.0.1",
    "port": {{ app_port }}
  },
  "database": {
    "host": "{{ db_host }}",
    "port": {{ db_port }},
    "user": "{{ db_user }}",
    "password": "{{ db_password }}",
    "database": "{{ db_name }}"
  }
}
